name: Visual Hive Live Detection

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  live-detection:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Checkout demo site
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4

      - name: Checkout Visual Hive
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4
        with:
          repository: DavidDiaz0317/visual-hive
          ref: 59ca827ea5955bcd4459eb9ae203e9703533226a
          path: visual-hive-tooling

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22
          cache: npm

      - name: Build Visual Hive
        run: |
          npm ci
          npm run build
        working-directory: visual-hive-tooling

      - name: Install demo dependencies
        run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Build and typecheck demo site
        run: |
          npm run build
          npm run typecheck

      - name: Run live Visual Hive detection
        env:
          VISUAL_HIVE_CLI: visual-hive-tooling/packages/cli/dist/index.js
        run: |
          npm run vh:live-detect
          npm run vh:mcp
          npm run vh:mcp:smoke
          npm run vh:artifacts
          npm run vh:path-scan
          # Writes .visual-hive/baselines.json before artifact upload.
          node "$VISUAL_HIVE_CLI" baselines list --config visual-hive.config.yaml --write
          npm run vh:report -- --github-step-summary

      - name: Upload Visual Hive live artifacts
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: visual-hive-live-detection
          path: .visual-hive
          if-no-files-found: error
          include-hidden-files: true
